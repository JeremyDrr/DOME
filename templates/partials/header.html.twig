<nav class="navbar navbar-dark navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path("homepage")}}">DOME</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path("homepage") }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ads_index') }}">Browse</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}#about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}#contact">Contact</a>
                </li>
            </ul>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="accountDropdownLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="{{ app.user.fullName }}'s avatar"> {{ app.user.fullName }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-dark" aria-labelledby="accountDropdownLink">
                            {% if is_granted('ROLE_ADMIN') %}
                                <a href="" class="dropdown-item">Admin Panel</a>
                                <div class="dropdown-divider"></div>
                            {% endif %}
                            <a href="{{ path('ads_create') }}" class="dropdown-item">Create an offer</a>
                            <a href="{{ path('account_index') }}" class="dropdown-item">My account</a>
                            <a href="{{ path('account_edit') }}" class="dropdown-item">Edtit my data</a>

                            <a href="{{ path('account_logout') }}" class="dropdown-item">Logout</a>

                        </ul>

                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('account_register') }}" class="nav-link">Sign Up</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('account_login') }}" class="nav-link">Sign In</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>